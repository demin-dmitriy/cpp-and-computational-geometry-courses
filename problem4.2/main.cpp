#include <iostream>
#include <Windows.h>

#include "list.h"

using namespace sort_list;
typedef list<43, list<52, list<21, list<98, list<8493, list<-329, list<0, list<4, list<38, list<92, list<-3, list<100, null> > > > > > > > > > > > sample;

typedef sort<sample>::res::res out;

const int input[] = {at<sample, 0>::res, at<sample, 1>::res, at<sample, 2>::res, at<sample, 3>::res, at<sample, 4>::res, at<sample, 5>::res,
                     at<sample, 6>::res, at<sample, 7>::res, at<sample, 8>::res, at<sample, 9>::res, at<sample, 10>::res, at<sample, 11>::res}; 

const int output[] = {at<out, 0>::res, at<out, 1>::res, at<out, 2>::res, at<out, 3>::res, at<out, 4>::res, at<out, 5>::res,
                      at<out, 6>::res, at<out, 7>::res, at<out, 8>::res, at<out, 9>::res, at<out, 10>::res, at<out, 11>::res}; 


int main()
{
	/*
	for (size_t i = 0; i < size<sample>::res; ++i)
	{
		std::cout << input[i] << ' ';
	}

	std::cout << "\n\n";

	for (size_t i = 0; i < size<out>::res; ++i)
	{
		std::cout << output[i] << ' ';
	}

	std::cout << "\n\n";

	system("pause");
	*/

	return 0;
}